{
  "sources": {
    "strava": {
      "enabled": true,
      "note": "Official Strava OAuth API. Credentials come from .env."
    },
    "mynetdiary": {
      "enabled": true,
      "note": "Reads the official CSV export you drop into imports/."
    },
    "liftoff": {
      "enabled": false,
      "note": "Reads a workout JSON export you drop into imports/ (liftoff_*.json). Off by default; enable with --enable-liftoff. Personal use only."
    }
  }
}
